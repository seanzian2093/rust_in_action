// This package is not working due to package issue. Still working on it
mod ethernet;
mod http;

use clap::{Command, Arg};
// use smoltcp::phy::TapInterface;
use url::Url;


fn main() {
    println!("Hello, world! from ch8-mget");
}
